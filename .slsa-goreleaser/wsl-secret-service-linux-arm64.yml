version: 1

goos: linux
goarch: arm64
binary: wsl-secret-service-{{ .Os }}-{{ .Arch }}
main: ./cmd/wsl-secret-service

env:
  - CGO_ENABLED=0
  - GOEXPERIMENT=runtimesecret

flags:
  - -trimpath
  - -buildmode=pie

sboms:
  - documents:
      - "{{ .Binary }}.sbom.json"
