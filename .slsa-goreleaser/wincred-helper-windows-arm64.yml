version: 1

goos: windows
goarch: arm64
binary: wincred-helper-{{ .Os }}-{{ .Arch }}
main: ./cmd/wincred-helper

env:
  - CGO_ENABLED=0

flags:
  - -trimpath
  - -buildmode=pie

sboms:
  - documents:
      - "{{ .Binary }}.sbom.json"
